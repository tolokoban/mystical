export default class WordsTree {
    private readonly root;
    constructor(words: string[]);
    find(input: string): string[];
    private addWord;
    toString(): string;
}
//# sourceMappingURL=words-tree.d.ts.map